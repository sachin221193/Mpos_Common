version: '2'
services:
    common-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb common-mongodb-config
    common-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    common-mongodb-config:
        image: mongo:3.6.3
        container_name: common-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
